<?xml version='1.0' encoding='utf-8'?>
<widget id="<%= BUNDLE_ID %>"
        version="<%= BUNDLE_VERSION %>"
        android-versionCode="<%= BUNDLE_VERSION.replace(/\./g, '') %>"
        ios-bundleVersion="<%= BUNDLE_VERSION %>"
        xmlns="http://www.w3.org/ns/widgets"
        xmlns:cdv="http://cordova.apache.org/ns/1.0">

    <name><%= BUNDLE_NAME %></name>

    <description> <%= BUNDLE_DESC %> </description>

    <author email="<%= PUBLISHER_EMAIL %>" href="<%= PUBLISHER_URL %>">
      <%= PUBLISHER %>
    </author>

    <content src="index.html" />

    <!-- Security options -->
    <!-- (see https://github.com/apache/cordova-plugin-whitelist for details) -->

    <!-- We'll optionally navigate to a browsersync server - allow for it, if exists -->
    <% if(HTTP_SERVER_IP && HTTP_SERVER_PORT) { %>
    <allow-navigation href="http://<%= HTTP_SERVER_IP %>:<%= HTTP_SERVER_PORT %>/index.html" />
    <% } %>

    <!-- Don't block any requests -->
    <access origin="*" />

    <!-- Some small old and new fixes for cordova  -->
    <!-- (some of them aren't needed anymore, but let's keep them here anyway) -->
    <preference name="fullscreen" value="true" />
    <preference name="webviewbounce" value="false" />
    <preference name="UIWebViewBounce" value="false" />
    <preference name="DisallowOverscroll" value="true" />
    <preference name="KeyboardShrinksView" value="true" />

    <preference name="orientation" value="landscape" />
    <preference name="SplashScreen" value="screen" />
    <preference name="AutoHideSplashScreen" value="true" />

    <!-- Fix error with "error while loading asset index.html" -->
    <preference name="LoadUrlTimeoutValue" value="70000" />

    <!-- Plugin configuration: https://github.com/driftyco/ionic-plugin-keyboard -->
    <feature name="Keyboard">
        <param name="ios-package" value="IonicKeyboard" onload="true" />
    </feature>

    <platform name="ios">
      <!-- images are determined by width and height. The following are supported -->
      <!--<splash src="platforms/ios/owlstand devDbg/Resources/splash/Default~iphone.png" width="320" height="480"/>-->
      <!--<splash src="platforms/ios/owlstand devDbg/Resources/splash/Default@2x~iphone.png" width="640" height="960"/>-->
      <!--<splash src="platforms/ios/owlstand devDbg/Resources/splash/Default-Portrait~ipad.png" width="768" height="1024"/>-->
      <!--<splash src="platforms/ios/owlstand devDbg/Resources/splash/Default-Portrait@2x~ipad.png" width="1536" height="2048"/>-->
      <!--<splash src="platforms/ios/owlstand devDbg/Resources/splash/Default-Landscape~ipad.png" width="1024" height="768"/>-->
      <!--<splash src="platforms/ios/owlstand devDbg/Resources/splash/Default-Landscape@2x~ipad.png" width="2048" height="1536"/>-->
      <!--<splash src="platforms/ios/owlstand devDbg/Resources/splash/Default-568h@2x~iphone.png" width="640" height="1136"/>-->
      <!--<splash src="platforms/ios/owlstand devDbg/Resources/splash/Default-667h.png" width="750" height="1334"/>-->
      <!--<splash src="platforms/ios/owlstand devDbg/Resources/splash/Default-736h.png" width="1242" height="2208"/>-->
      <!--<splash src="platforms/ios/owlstand devDbg/Resources/splash/Default-Landscape-736h.png" width="2208" height="1242"/>-->
    </platform>
</widget>
