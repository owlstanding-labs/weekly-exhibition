ion-view
  ion-nav-title {{ title() }}
  ion-content
    iframe(width='100%' height='100%'
      ng-src='{{ url }}'
      frameborder='0' allowfullscreen)
    button.button.button-light(ng-click='prev()') Prev
    button.button.button-light(ng-click='next()') Next
